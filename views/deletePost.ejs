<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Post</title>

    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }

        /* Body Styling */
        body {
            background-color: #121212;
            color: #e0e0e0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
            padding: 20px;
        }

        /* Confirmation Heading */
        h2 {
            color: #ff4d4d;
            margin-bottom: 20px;
            font-size: 1.8rem;
        }

        /* Form Container */
        #formcontainer {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 15px;
            font-size: 1.2rem;
            color: #ff4d4d;
            background-color: #1e1e1e;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            max-width: 400px;
            width: 100%;
        }

        /* Input Fields */
        input[type="password"] {
            padding: 10px;
            font-size: 1rem;
            outline: none;
            border-radius: 6px;
            border: none;
            background: #2e2e2e;
            color: #e0e0e0;
            width: 100%;
            text-align: center;
        }

        /* Delete Button */
        #delbutton {
            background-color: red;
            color: white;
            font-size: 1.1rem;
            font-weight: bold;
            border: none;
            cursor: pointer;
            padding: 12px;
            border-radius: 6px;
            width: 100%;
            transition: background 0.3s, transform 0.2s;
        }

        #delbutton:hover {
            background-color: #cc0000;
            transform: scale(1.05);
        }

        /* Responsive */
        @media (max-width: 600px) {
            #formcontainer {
                max-width: 90%;
            }

            h2 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <h2>Confirm Post Deletion</h2>
    <div class="container">
        <% if (post) { %>
        <form action="/delete/<%= post.id %>" id="formcontainer" method="POST">
            <label for="password">Enter Password to Confirm</label>
            <input type="password" name="password" placeholder="Enter your password" id="inppass" required>
            <input type="submit" value="Delete Post" id="delbutton">
        </form>
        <% } %>
    </div>
</body>

</html>
